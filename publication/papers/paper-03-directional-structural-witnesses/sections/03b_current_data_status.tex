\subsection{Current confirmatory-data status}

The strong semantic-control execution lane has advanced since the original
manuscript freeze.  The exact \path{BAAI/bge-reranker-v2-m3} asset was staged
on FS9 and subsequent label-blind descriptor jobs 3476527--3476529 reached
\path{HARVEST_DESCRIPTOR_READY}.  Sixteen v2.1 source--target items therefore
have content-bound semantic descriptors available under the preserved
zero-label chronology.  This is execution preparation only:
\path{training_authorized=false}, no witnessed-structure model comparison has
been scored against external labels, and no confirmatory transfer metric is
licensed by descriptor readiness alone.

The external-evidence gate remains open.  The original public annotation issue was deleted during automation and has been recreated as issue \#217 with the same human-only contract.  Two genuinely distinct annotators must independently label all 16 items; a distinct adjudicator may act only after both responses are frozen; and a separate provenance auditor must verify role separation, expertise/conflict metadata and chronology.  At the current evidence cutoff issue \#217 remains open and no qualifying response, adjudication or provenance-audit artifact exists in the public repository.

Consequently the confirmatory question is still unanswered: whether witnessed
structural features add predictive value beyond the strongest frozen
non-structural control on independently judged natural-domain cases.  The
missing data are human labels and adjudication, not additional same-session
model generations.  Until those artifacts exist, the correct paper status is
\path{BLOCKED_ON_HUMAN_ANNOTATION}; training/inference and confirmatory
Paper-III numbers remain fail-closed.

\subsection{Pre-label power design}

Before any external judgement, a label-blind power study (\#248) registered a
primary paired Brier-reduction MDE of 0.05 and simulated paired bootstrap/sign-flip
resolution under plausible item-level noise.  The frozen v2.1 sixteen-item packet
is \emph{underpowered} for that MDE: adequate resolution requires roughly
$n\approx 48$ items across the registered noise envelope, whereas the solicited
confirmatory set remains $n=16$.  The study therefore scopes external validation as
\path{CONFIRMATORY\_PACKET\_POWER\_LIMITED}: indistinguishable outcomes are
inconclusive rather than negative evidence, and confirmatory refutation claims
remain blocked when intervals include zero.
