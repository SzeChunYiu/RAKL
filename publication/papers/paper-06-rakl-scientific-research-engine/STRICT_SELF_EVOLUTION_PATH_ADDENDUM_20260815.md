# Paper VI / Self-RAKL — strict current evolution path (2026-08-15)

Status: binding local-closure addendum. Historical Self-RAKL v1-v4 modules remain immutable research/evidence surfaces, but **they are not the current closure-eligible recursive-evolution facade**.

The current local path is `src/rakl/meta_evolution_v5.py`.

It accepts only:

- failure families represented by v4 content-manifest mutation contracts;
- canonical v4 contexts whose domain/problem-family/substrate/evaluator identities are SHA-256 manifest digests;
- contextual mutation credit keyed by operator-contract digest × target layer × canonical context digest;
- cross-context credit only through an exact content-addressed transport witness;
- decision-bearing discriminator receipts whose diagnosis/discriminator/evidence identities are SHA-256 digests;
- exact Git subject identity plus content-addressed evaluator/dependency/metric/benchmark/environment/cutoff identities for higher-order assurance;
- hard validity before any soft Pareto comparison.

The facade converts these strict objects into the historical controllers only after the content-addressed contract has passed. A caller presenting a free scope string, renamed failure-family label, display-name evaluator or weaker historical portrait is not a valid current strict input.

Passing the strict facade means only that a challenger may enter the existing protected method-promotion gate. It grants no scientific or method-promotion authority. The mapping from an external evaluator, benchmark, actor or run to the bytes represented by a digest remains an explicit external provenance trust root and is not self-certified locally.
