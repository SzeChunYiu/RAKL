# Strict ORION closure addendum — 2026-08-14

This addendum supersedes the paper's earlier status-box wording where it conflicts with the evidence below; it does not rewrite historical results.

1. Corrected Phase-0/1 v2 A100 evidence contains a 7B `state_reachability` `MECHANISM_SIGNAL_PRESENT` result (principle mastered at exposure 2, late same-structure gain 0, unsaturated-coordinate late gain +0.25). The historical v1 blanket negative remains an invalid-instrument artifact.
2. The vector training projection and fail-closed allocator are implemented, but implementation is not efficacy authority.
3. A fresh 384-world model-free RSHEA development stress found adaptive-v1 worse than the static structural parent on balanced mastery and hard-safety minimum. This result is preserved as `DEVELOPMENT_NEGATIVE_ADAPTIVE_OVERCONCENTRATES__STATIC_PARENT_RETAINS_DEFAULT`.
4. ORION therefore does not keep adaptive-v1 as a required failed dependency. `STATIC_STRUCTURAL` is the active training-policy default. Adaptive can become active only under a fresh external `ADAPTIVE_RESIDUAL_SUPPORTED` receipt that also passes strongest-parent, hard-harm, fresh-assurance and full-overhead gates.
5. The exact five-arm Qwen2.5-7B Phase-2 protocol remains frozen assurance. The previous session-level resource block was localized to execution portability, and a versioned LUNARC A100 route now runs the unchanged protocol from the already staged exact model revision. No Phase-2 scientific outcome is asserted until that external job is actually executed and harvested.
6. Standalone positive Paper IV remains conditional on issue #462. ORION's active framework operation does not depend on that unproven adaptive extension.

Nothing in this addendum grants scientific or theorem authority.
