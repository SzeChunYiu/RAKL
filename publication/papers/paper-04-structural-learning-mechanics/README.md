# 4 structural learning mechanics

Self-contained package. Build: `pdflatex main && bibtex main && pdflatex main && pdflatex main`.
```
main.tex   # entry point (single-file manuscript; no external data)
```
Figures (KPI/mastery trajectory) are added when the Phase-1 (#461) experiment produces real exposure-outcome data; see docs/ORION_KPI_AND_METRICS.md and figures/scripts/ once populated.
