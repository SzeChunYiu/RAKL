# Remaining fresh empirical gates

The framework specification is closed relative to this audit, but two empirical gates remain intentionally open.

## Gate A — RFA-v1 fresh utility

Known-world differential conformance establishes implementation behavior, not scientific usefulness.

Freeze a fresh task set before outcomes and compare at minimum:

- direct/static problem solving;
- current RAKL without recursive formulation;
- generic self-reflection parent;
- full RFA-v1 controller.

Primary endpoints should measure whether the controller identifies the responsible ancestor coordinate and improves decision quality/cost without raising authority or erasing negatives. Evaluator and task labels must be frozen before solver execution.

Terminal until run:

```text
RFA_FRESH_UTILITY_ASSURANCE = OPEN_EMPIRICAL
```

## Gate B — semantic acquisition successor

The local Paper-II representation lane has not established a capable reducer. The next materially different route is a registered semantic acquisition parent.

A remote Hugging Face Jobs execution attempt in this audit returned HTTP 402 Payment Required. Therefore:

```text
SEMANTIC_PARENT_EXECUTION = CANNOT_CHECK_RESOURCE_BOUND
```

Do not count this as a semantic-parent negative. When the resource is available, freeze the model/normalizer, SCAR split, input regime, evaluator, thresholds, and contamination policy before execution.

## Gate C — full-corpus observation-contract audit (optional but publication-useful)

The 12-record SCAR diagnostic is enough to justify declaring the information regime, but not enough for a corpus-level prevalence claim. If Paper II wants a quantitative claim about mismatch frequency, run a preregistered full-corpus or statistically sampled audit with independent double coding and adjudication.
