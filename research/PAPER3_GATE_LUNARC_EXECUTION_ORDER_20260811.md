# Paper 3 gate and LUNARC execution order

Date: 2026-08-11
Authority: same-context engineering and research audit; not independent review or peer review.

## Frozen object and decision

The object is directional, context/QoI-scoped structural reuse.  The immediate
decision is whether evidence authorizes any Paper 3 model training or frozen-model
inference on LUNARC.  It does not: the v2.1 packet has zero registered external
judgements, no adjudication and no external provenance audit.  Issue 43 is an open
solicitation with zero comments at the recorded audit time, not annotation evidence.

## Cheap-gate finding

The v1 constructed diagnostic retained an incremental signal against its strongest
implemented control, the dependency-aware Jaccard/tag-overlap arm.  That observation
is label-visible internal evidence only.  The fresh v2 confirmatory gate has not run.

The hostile control audit also leaves a publication-level blocker.  The current
implemented control sequence is calibrated surface Jaccard, skill-tag Jaccard and
dependency-pattern Jaccard.  Before final confirmatory promotion, and while the
external outcomes remain unseen, the programme must freeze a modern dense or
cross-encoder semantic control and faithful parent controls for the applicable
Skill-It/MASS/SWIFT mechanisms.  A weak semantic baseline cannot mint structural
novelty.  Existing v2 thresholds and negative history remain protected.

## Exact order

1. Keep the external packet and access chronology frozen.
2. Obtain two role-separated external annotations per item.
3. Freeze both submissions, then obtain distinct adjudication and an external
   provenance audit.
4. Freeze the stronger semantic/skill/dependency parent implementations before
   adjudicated outcomes are opened to their evaluator.
5. Run the cheap family-held-out signal gate.  Missing evidence or a threshold
   failure stops compute and narrows the claim.
6. Only after a schema-valid overall pass, run the allocated-node preflight.
7. Submit the separate training template, recording task-level and resource receipts.
8. Freeze the resulting or parent model weights, then submit the separate inference
   template.  Any input-weight mutation fails the inference receipt.
9. Compute break-even only from training, inference, induction, retrieval, adaptation
   and verification receipts.  A no-break-even result remains visible.

## LUNARC binding

Access was observed through Tailscale host `billy-old` and its active persistent
LUNARC socket.  The canonical output root is
`/projects/hep/fs9/users/scyiu/RAKL-paper3`.  Account `lu2026-2-51` is associated
with `gpua100`, `gpua100i`, `gpua40` and `gpua40i`.  This iteration submitted zero
SLURM jobs.

The training and inference templates are deliberately not self-submitting.  The
submission wrapper emits a machine-readable refusal unless the gate, clean Git
subject, FS9 destination, artifact hashes, model mode, runner, result schema and
workload-specific batch-script allowlist all agree.  The allocated node repeats the
checks with explicit exceptions that remain active under `python -O`, and hashes the
input weights before and after the workload.
