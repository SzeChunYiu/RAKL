# R004 route delta after C009

**Date:** 2026-08-10  
**Authority:** route-control update only

C009 resolves the canonical-cover discriminator opened after the merged perfect-matching checkpoint C007.

## Canonical lane closed as a super-log target

For every square bipartite graph `G subseteq [N] x [N]`, with complement `U=G^c` and maximum matching number `nu(U)`, C009 gives the proof-draft ceiling

`rho_can(G) <= ceil(log_2 nu(U)) <= ceil(log_2 N)`.

The source calibration `G_NEQ` attains the logarithmic scale when `N=2^n`. Therefore no Hall-deficient, expander, Cayley, random-like, algebraic, or other square graph family can produce a **super-logarithmic canonical** lower bound.

The previous Hall-deficient canonical search is retired. The exact canonical oracle remains only for regression, local falsification, and theorem calibration.

## Why R004 remains active

Cavalar-Oliveira full cover complexity quantifies over all relevant semi-filters, not only canonical row/column-generated filters. The source proves random graphs have linear full cover complexity. C009 therefore identifies a strict insufficiency of the canonical subfamily rather than an upper bound on the source's full problem.

## Active successor

The next atomic object is:

> identify the smallest compact, source-complete **noncanonical** semi-filter family or invariant that can witness full cover complexity and still admits exact falsification.

The construction must come with an exact relation to the source full-cover definition before any lower-bound search is trusted.

## Failure rule

Do not respond to the canonical ceiling by brute-force enumeration of unconstrained objects without a source-bound completeness argument. If a candidate noncanonical subclass is not proven to lower-bound or faithfully represent full cover complexity, it is only a conjecture generator.

## Parallel route status

R002 remains open through non-black-box/Gap-MCSP amplification and non-local high-threshold magnification. The canonical R004 lane is closed, but the full-cover R004 lane remains active and independent.
