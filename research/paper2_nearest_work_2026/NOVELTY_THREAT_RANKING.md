# Novelty threat ranking scaffold (#487)

Status: `NOT_RANKED`

For each Paper II residual claim, assign RED / AMBER / GREEN with evidence pointers.

## Candidate residual under test

> A directional, evidence-bearing **transport contract** for reusable research experience that explicitly binds QoI, source preconditions, role/relation mapping, preserved invariants, forbidden losses, target boundaries and uncertainty, optimized for fail-closed ACCEPT/REJECT/CANNOT_CHECK rather than analogy similarity generation.

## Systems residual (optional, stronger)

> Same frozen witness consumed by research router (`JUMP`/`GLUE`) with downstream safety/utility vs semantic and mechanism-alignment controls.

Threat table: *to be filled after CLAIM_MATRIX rows exist*.
