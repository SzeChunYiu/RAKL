# Comparator requirements scaffold (#487)

Status: `PROPOSAL_ONLY`

Confirmatory Paper II / #444 objective lane should include, where feasible:

- strong semantic reranker / cross-encoder
- relation / system-similarity baseline
- mechanism-alignment LLM judge with matched budget (2026 analogical-research motivated)
- structure-mapping / graph matching baseline where inputs permit
- full RAKL StructuralWitnessV2 gate
- ablations: directionality, QoI/boundaries, preconditions, forbidden-loss/disanalogy

If a simpler parent matches the full witness, absorb it and narrow the paper.
