You are given two scientific-system excerpts. Decide whether the source and target support a useful analogy for the stated comparison, and identify the corresponding concepts if they do.

Use only the supplied excerpts. Do not assume missing facts. Return exactly one JSON object with keys:
- decision: one of LICENSED, REJECTED, CANNOT_CHECK
- mappings: a list of [source_term, target_term] pairs

If decisive target information is absent, use CANNOT_CHECK. If the analogy is contradicted, use REJECTED. Do not include prose outside the JSON object.
