# AI session handoff — RAKL × LLM Structural Bridge

## Start here

You are inheriting a **research programme with mixed evidence states**, not a positive-result narrative. Preserve all negative/retracted history and do not widen claims without a fresh gate.

Read in this order:

1. `research/llm_structural_bridge_20260813/README.md`
2. `research/llm_structural_bridge_20260813/NOVELTY_MATRIX.md`
3. `research/llm_structural_bridge_20260813/EXPERIMENT_LADDER.md`
4. `research/llm_structural_bridge_20260813/DEVELOPMENT_RESULT.json`
5. `research/llm_structural_bridge_20260813/known_world_neural_bridge.py`
6. `src/rakl/structural_types.py`
7. `src/rakl/training_projection.py`
8. `research/tcsq_v0/`
9. `publication/papers/paper-02-structural-mechanics/sections/03c_objective_confirmatory_result.tex`
10. `research/llm_comparator_confirmatory_v1/`
11. `publication/papers/paper-04-structural-learning-mechanics/main.tex`
12. `experiments/training_ladder/phase1_v2.py`

## Non-negotiable chronology

### Phase-1 training v1 is retracted

Do **not** use the old Qwen v1 terminals in `research/paper4_phase1_results/README.md` as evidence for or against learner-conditioned structural saturation. The current paper records two independent instrument defects:

- only two unique generator inputs per family;
- masked gold answer token, so no gradient reached the VALID/INVALID decision.

The corrected v2 instrument is the only authorized successor. A null/positive training conclusion requires the full corrected v2 rerun, not reinterpretation of v1.

### Neural Bridge CPU result is development only

The five-seed result in `DEVELOPMENT_RESULT.json` establishes:

- QoI-conditioned geometry is learnable in an exact synthetic world;
- the learned geometry can be reused for fresh-domain nearest-neighbor retrieval;
- explicit boundary supervision can reject same-surface boundary decoys;
- **but a generic conditional metric learner already explains most of the effect**.

Terminal is exactly:

`FEASIBILITY_SUPPORTED_RAKL_SPECIFIC_RESIDUAL_NOT_ESTABLISHED`

Do not rename it.

## Evidence that is already meaningful

### Structural applicability known world

The full RAKL applicability contract has a preregistered exact-verifier result over 576 generated cases/four families. The full contract matches all decisions; mechanism-only and relation/invariant-only controls leave invalid false accepts. This establishes the necessity/value of the *full applicability coordinates in that exact scope*, not raw-prose extraction or natural-domain superiority.

### External LLM gate

A frozen GLM-5.2 exact-verifier comparator (`n≈504`) finds:

- Direct invalid false accept: 0.527 [0.460,0.589]
- Free-CoT: 0.536 [0.473,0.603]
- RAKL gate: 0.339 [0.277,0.402]

The Free-CoT control is important: extra reasoning alone does not explain the gate effect. Hardest semantic-near decoys remain a weakness and must be retained.

### TCSQ

SQ-1 oracle quotient: 720 cases, 4 families, exact verifier agreement 1.0, ~25.3% public primitive reduction.  
SQ-2 registered finite interventions: 144 cases, exact dependency recovery; precision/recall/specificity 1.0.

These justify SQ-3. They do not establish learned quotient discovery or total-cost savings.

## High-novelty residuals to pursue

### 1. TCSQ SQ-3 — highest current priority

Run the already registered matched solver-cost/quality test. Strong controls must include:

- raw problem;
- structure-without-erasure;
- generic compression/summarization;
- Context-Codec-style commitment-preserving compression;
- formal slice/state-abstraction where faithful;
- mechanism-only relational abstraction;
- incumbent RAKL;
- TCSQ;
- oracle quotient.

Primary quantity must be **net benefit after quotient extraction, validation, reconstruction and original verification cost**. If overhead erases savings, report no efficiency win.

### 2. Neural TCSQ confirmatory known world

Freeze a fresh generator/version before results. The load-bearing contrast is:

`RAKL_TCSQ_NEURAL - strongest(conditional metric, matched counterfactual metric, relational bottleneck/Abstractor, IIT/causal abstraction)`

Do not use the existing development generator as fresh assurance.

Must contain:

- QoI flip on the same pair;
- boundary-only near-miss;
- protected-coordinate interventions;
- erasable-coordinate interventions;
- unseen factor compositions;
- unseen domain shells;
- `CANNOT_CHECK` cases;
- source/reconstruction/original-verifier failures.

If the parent matches RAKL, assimilate the parent and move the novelty to governance/shared-substrate semantics.

### 3. Witness-aligned neural transfer

Build a separate known world specifically for **one-way, partial transfer**. Test explicit `non_preserved_properties`, target boundaries and direction. Include ablations that remove each of these fields.

Primary harm metric: invalid-transfer false accept on fresh direction×boundary×QoI interactions.

A positive direct accuracy gain with increased false transfer is a failure.

### 4. Corrected structural-learning Phase-1 v2 rerun

Before any adaptive scheduler:

- use `phase1_v2.py` only;
- re-check prompt-level disjointness;
- require learnability positive-control pass;
- bind exact model/checkpoint/tokenizer/optimizer/generator hashes;
- run registered exposure counts;
- preserve all family-level trajectories;
- produce a typed terminal.

Only `MECHANISM_SIGNAL_PRESENT` (or a separately frozen equivalent) can authorize #466.

### 5. Small open-model Neural Bridge

Only after a known-world parent residual survives. Start with LoRA/adapters; do not train a foundation model from scratch.

Use the smallest preregistered model that clears a capability screen, not whichever model makes RAKL win. Match base checkpoint, examples, optimizer, token/FLOP ceiling and evaluation splits.

### 6. Exact train→inference identity reuse

If a learner-side mechanism works, freeze a completely fresh inference task set and reuse the exact structural catalog/IDs/schema from training. Include ID-only and separately learned-representation ablations.

If a new unrelated representation is required, terminal is `TWO_MECHANISMS_NOT_SHARED_SUBSTRATE`.

### 7. Epistemically governed cognitive compilation

Do this last. Compare against generic reflection+distillation, generic failure-example fine-tuning and textual-skill→LoRA parents. The proposed distinctive transition is:

`verified residual -> typed diagnosis -> bounded training proposal -> challenger -> fresh assurance -> promote/reject/narrow`

Training success must never rewrite scientific evidence roots or authority.

## Strongest prior-art threats that must stay in every discussion

- Conditional Similarity Networks — arXiv:1603.07810
- Causal Abstractions — arXiv:2106.02997
- IIT — arXiv:2112.00826
- Abstractors — arXiv:2304.00195
- Relational Bottleneck — arXiv:2309.06629
- MATES — arXiv:2406.06046
- Group-MATES — arXiv:2502.14709
- STAT — arXiv:2510.10023
- STEPS — arXiv:2601.03676
- Structural information in LMs — arXiv:2601.17869
- Emergent Analogical Reasoning — arXiv:2602.01992
- Context Codec — arXiv:2605.17304
- LatentSkill — arXiv:2606.06087
- SciForma — arXiv:2607.18091
- Darwin Gödel Machine — arXiv:2505.22954
- Hyperagents — arXiv:2603.19461
- Instruction-Level Weight Shaping — arXiv:2509.00251

Search again before publication freeze; this list is only current to 2026-08-13.

## Claim language that is currently defensible

Safe:

> RAKL already has controlled evidence that an explicit full applicability contract can resolve exact structural-transfer decisions that simpler mechanism/relation projections miss, and a one-model external comparator suggests that driving an LLM through those obligations reduces invalid transfer beyond a compute-matched free-CoT control. Separately, TCSQ has exact known-world evidence for decision-sufficient quotienting and finite dependency recovery. A new development neural screen shows that QoI-conditioned structural geometry is learnable and reusable for fresh-domain retrieval, but a strong generic conditional-metric parent explains most of that signal, so an RAKL-specific neural residual remains unestablished.

Unsafe:

- “RAKL has proven a new LLM architecture.”
- “RAKL adaptive training works.”
- “RAKL invented task-conditioned similarity.”
- “RAKL is the first system to use structure at training and inference.”
- “The neural bridge improves LLM reasoning.”
- “The Qwen ladder disproved/supported learner-conditioned saturation.”

## Desired end state

The high-value result is not a collection of positive demos. It is one of two clean outcomes:

### Strong success

A validated RAKL quotient/witness representation adds fresh structural-OOD value beyond the strongest conditional/relational/causal parent; the same frozen identities remain useful after weight training at inference; total cost and hard boundary/retention constraints pass; governed external failures can later produce beneficial weight challengers.

### Clean narrowing

Strong parents explain the neural effect or shared identity adds no value. In that case retain TCSQ/external applicability results, assimilate the parent mechanism, and explicitly reject the broader RAKL-native-LLM claim.

Both outcomes are scientifically useful. Do not optimize the research process for a positive terminal.