# Neural Structural Bridge — residual preregistration

## Two-object architecture

Do not collapse:

1. **TCSQ quotient/equivalence representation** — task/QoI/context-conditioned irrelevant distinctions;
2. **StructuralWitness transport/gate** — asymmetric source→target applicability, boundaries and non-preservation.

If fresh evaluation contains reversed pairs with different directional labels, any scorer that depends only on a symmetric pair similarity has a known representational ceiling. Report that ceiling but still compare against strong **asymmetric** parents.

## Experiment A — Neural TCSQ

Arms:

- raw;
- unconditioned structural;
- strongest matched generic conditional metric;
- matched-information/counterfactual-augmentation control;
- explicit RAKL TCSQ contract.

Fresh tests: domain shift, QoI flip, protected-coordinate trap, boundary near-miss, quotient sufficiency violation.

Positive result required: RAKL residual beyond strongest parent under matched examples/compute. Development known-world feasibility alone is insufficient.

## Experiment B — directional witness

Ablations:

- reverse direction;
- remove/flip boundaries;
- remove non-preserved-property ledger;
- corrupt role map;
- corrupt invariant/relation preservation.

Strong parents: asymmetric relational scorer, relational bottleneck/Abstractor-style parent, causal-abstraction parent.

## Shared-identity phase

Only after mechanism residual: use the exact same frozen `StructuralIdentityBundle` in external reasoning, learner projection/training, and inference retrieval/transport. Fresh tasks must be disjoint.

## Open-model phase

Only after known-world residual. Use identical examples, optimizer/compute budget and fresh structural OOD panel. Any LoRA/adapter result must bind checkpoint, tokenizer, optimizer, sampling, split and code hashes.
