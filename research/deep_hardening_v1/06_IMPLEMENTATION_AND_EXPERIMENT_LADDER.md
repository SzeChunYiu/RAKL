# Revised implementation and experiment ladder

The order prevents downstream complexity from hiding upstream falsification.

## Stage 0 — Reconcile live repository

- re-fetch `main` and compare with frozen base;
- inspect current open-gap register, issues/PRs and CI;
- run current full suite before applying this packet;
- preserve all retracted/negative history.

If baseline is red, record it before edits.

## Stage 1 — Integrate additive hardening contracts

Port:

- exact-base deterministic guard edits (structural types, TCSQ finite/conditional-erasure guards, scientific evidence lineage);
- canonical commitment + V3 dual-write;
- resolved TCSQ validation assurance sidecar;
- training assurance sidecar;
- witness use-site contract;
- approximation budget;
- diagnosis state machine;
- authority assurance;
- identity bridge;
- VTG typed contracts;
- cognitive compilation contracts;
- unified integration manifest/readiness gate;
- hardening overlay components that are not already superseded on main.

Do not replace existing `semantic_quotient`, `path_congruence`, `cost_geometry`, `proof_dag`, `navigation_quotient`, `structural_types`, `v3_scientific_authority` with older handoff copies.

## Stage 2 — TCSQ SQ-3

Primary question: does quotienting reduce **total** solution cost after including:

```text
construction + sufficiency validation + reconstruction + original verification
```

Controls: unquotiented solver, simple compression/slicing/abstraction parents, matched compute. Kill the practical-benefit claim if no net gain.

## Stage 3 — Neural TCSQ parent-residual known world

Freeze before outcomes:

- raw/unconditioned structural;
- strongest generic task-conditioned metric parent;
- matched-information augmentation control;
- explicit RAKL TCSQ objective;
- fresh domain + QoI flip + hostile boundary near-miss panels.

A positive development result is not an LLM claim.

## Stage 4 — Directional-witness neural known world

Keep separate from quotient geometry. Include reversed pairs with genuine one-way labels.

Ablate:

- direction;
- required target boundaries;
- non-preserved properties;
- role mapping;
- relation/invariant obligations.

Compare to strong asymmetric relational/causal parents, not only symmetric cosine/metric baselines.

## Stage 5 — Corrected structural-learning Phase-1 v2

Run the corrected varied generator/token-gradient instrument. Require:

- learnability positive control;
- train/probe disjointness;
- checkpoint-bound mastery;
- uncertainty over structural coordinates;
- retraction of v1 preserved in reports.

If no learner-conditioned signal survives, do not implement adaptive allocation as an efficacy claim.

## Stage 6 — Adaptive structural allocation

Only if Stage 5 passes. Primary estimand:

```text
ADAPTIVE_RAKL_STRUCTURAL - STATIC_RAKL_STRUCTURAL
```

Also include strongest skill-aware/model-aware parent. Match examples, tokens, optimizer steps and wall/compute budget.

## Stage 7 — Exact train → inference identity reuse

Compare exact shared identity against:

- semantically equivalent but independently reconstructed identity;
- structure-aware train only;
- structure-aware inference only;
- generic retrieval/skill controls.

Fresh tasks must be disjoint and stage bindings byte/content comparable.

## Stage 8 — VTG Phase 0 finite formal universe

No LLM solving. Build a bounded Lean universe with:

- exact state/environment/spec hashes;
- local kernel/replay transitions;
- proof DAG/joint-premise view;
- evaluator gold vs solver-visible map;
- train/dev/fresh theorem-family split;
- full construction-cost receipt.

## Stage 9 — VTG Phase 1 local navigation

Compare raw graph and strong learned/hand geometry parents under local/bounded-local policies. Primary metrics:

```text
verified success
states expanded
verifier calls
wall/compute
construction cost
route stretch
false descent
local minimum rate
N(k,B)
OOD/support failure
fresh-family transfer
```

If no net closed-loop signal exists: `NO_USEFUL_LOCAL_GEOMETRY_IN_REGISTERED_SCOPE`. Stop geometry dynamics work.

## Stage 10 — Multi-chart portals / abstraction challengers

Only if needed by failure diagnosis. Every portal binds preserved/non-preserved properties, boundaries and verifier receipt. Count portal build/verification cost.

## Stage 11 — Natural/adaptive dynamics

Only after geometry survives Stage 9. Compare flow/diffusion/Physarum/path-integral/stochastic sampling against best-first, A*, MCTS, equality saturation and the best learned proof-progress policy under matched total cost.

## Stage 12 — Small open-model LoRA/adapter experiment

Only after known-world neural/learner mechanism survives. Same examples/compute, fresh structural-OOD tests, no confirmatory target leakage.

## Stage 13 — Cognitive compilation

Compare structural-diagnosis compilation against:

- generic reflection + distillation;
- failure-example fine-tuning;
- textual skill → adapter/LoRA;
- no update;
- random matched update.

Require disjoint fresh assurance and no `pi_epi` movement during training.

## Stage 14 — Integrated research-workflow evaluation

Same base model, matched evidence cutoff and resource ceiling across simple prompting/RAG/generic agent/fixed RAKL/evolving RAKL. If simpler systems match quality/safety at lower total cost, the complex stack has not earned its engineering cost.
