# Prompt for the receiving AI session

You are integrating `RAKL_UNIFIED_DEEP_HARDENING_HANDOFF_20260813` into `SzeChunYiu/RAKL`.

## First action: reacquire authority

1. Fetch current `main` and record exact SHA/tree/time.
2. Compare with frozen base `3c24a9f78722ee5fa47ee3527e7e0e774aff91c6`.
3. If different, inspect changes before applying anything. Port by obligation, never force-apply.
4. Run current repository tests/audits first and preserve baseline output.

## Same-context expert cell

Use at least these role-separated passes and record each objection/disposition:

- formal semantics/rewrite/abstraction;
- neural representation/training;
- theorem-prover/replay engineer;
- systems/security/supply-chain;
- empirical/falsification/statistics;
- complexity/leakage/adversarial audit.

These roles are not independent reviewers.

## Integration rules

- current `main` beats stale handoff code;
- additive types/sidecars are preferred to breaking existing identities;
- no weakening of verifier/authority gates to make tests pass;
- preserve negative/retracted history;
- candidate/sampled/learned objects grant no theorem/scientific authority;
- every new closed gap needs a regression test and exact subject;
- every empirical “works” claim requires a preregistered fresh comparator experiment.

## Implementation tasks

1. On the exact frozen base, run `tools/apply_unified_handoff.py`; it verifies exact Git blob SHAs before editing the three reviewed current-main files. On a moved head, port those obligations manually.
2. Integrate/inspect `repo_patch/src/rakl/*` and tests.
3. Integrate non-conflicting `hardening_overlay/src/rakl/hardening` + schemas/tests.
4. Reuse existing `semantic_quotient`, `structural_types`, `path_congruence`, `navigation_quotient`, `cost_geometry`, `proof_dag`, `v3_scientific_authority`; do not replace them with old source.
5. Wire V3 canonical commitment as additive dual-write first.
6. Wire training assurance sidecar without changing legacy snapshot hashes.
7. Adopt `assess_transfer_for_use` at authority-sensitive transfer consumers so non-preservation becomes load-bearing.
8. Route production TCSQ materialization through the resolved-validation sidecar and add approximation-budget consumption to approximate downstream use.
9. Wire diagnosis state transitions into failure-driven repair as proposal-only mechanics.
10. Add exact structural identity bundle to future neural/training/inference experiments.
11. Keep VTG as `pi_solve`; it must not alter `pi_epi`.

## Research tasks, in order

TCSQ SQ-3 → Neural TCSQ residual → directional-witness residual → corrected Phase-1 v2 → adaptive allocation → exact train/inference identity reuse → VTG Phase 0/1 → only then natural dynamics/open-model experiments → cognitive compilation → integrated workflow evaluation.

## Required reporting

At end, produce:

```text
BASE_SHA
FINAL_SHA
FILES_CHANGED
TESTS_RUN
FULL_SUITE_RESULT
AUDIT_RESULT
PAPER_BUILD_RESULT
MIGRATION_STATUS
KNOWN_CODE_GAPS
OPEN_SCIENTIFIC_COORDINATES
CLAIMS_SUPPORTED
CLAIMS_BLOCKED
NEGATIVE_RESULTS_PRESERVED
INDEPENDENT_REVIEW_STATUS
NEXT_SINGLE_EPISTEMIC_CUT
```

Never report `GLOBAL_COMPLETENESS=true` from a finite audit.


## Cross-surface completion check

Before any end-to-end experiment, create a `UnifiedMechanicsManifest` for that epoch and require resolution only for the features actually under test. Do not interpret `READY_FOR_INTEGRATION_TEST` as scientific support.
