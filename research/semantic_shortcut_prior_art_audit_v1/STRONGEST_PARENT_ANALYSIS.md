# Strongest-parent analysis — semantic-shortcut system claim (#403)

**Authority:** `INTERNAL_PRIOR_ART_AUDIT`  
**Independent external review:** absent

## Attack construction

Attempt to reconstruct the RAKL mechanism by combining the strongest registered parents:

```text
Mostow/Hammond/Veloso (CBR + derivational analogy + repair + partial reuse)
+ PROMISE / LeanSearch-class structural proof retrieval
+ CEGIS / property-guided residual refinement
+ DreamCoder-class abstraction invention
+ agent skill/episodic memory
+ ordinary scientific provenance / proposal-vs-acceptance norms
```

## Attack answers

1. **Can one prior system already express `(O,T,O')` memory plus witnessed target applicability?**  
   No single registered source matches the typed content-bound episode + fail-closed target-applicability witness package. Closest fragments: Veloso derivational traces; PROMISE proof-state transitions. Verdict: `PARTIALLY_ANTICIPATED` at fragment level; `SYSTEM_COMBINATION_NOT_FOUND_WITHIN_REGISTERED_SEARCH` at full package.

2. **Is SEARCH/JUMP/GLUE merely renamed retrieve/adapt/compose?**  
   Functionally overlapping names exist in planning/CBR. The RAKL distinction under audit is the fail-closed full-effect vs partial-effect typing plus invention-last order with `CANNOT_CHECK`. Strongest-parent composition can approximate retrieve/adapt/compose, so broad conceptual priority is `CLAIM_TOO_BROAD`. The executable contract remains a bounded integration candidate.

3. **Does prior CBR/CEGIS already require an exhaustion condition materially equivalent to LIFT?**  
   Task-local search budgets, CEGIS loops and library-learning pressure are analogues, but the registered sources do not show the same compound gate: candidate-accounted rejection of SEARCH/JUMP/GLUE routes + cross-problem coverage receipt + repeated residual across ≥2 attempts + specification-only output. Verdict: `PARTIALLY_ANTICIPATED`.

4. **Is full-effect/partial-effect an ordinary planning composition rule?**  
   Partial-plan reuse is ordinary. Treating complete-effect routes and partial-effect compositions as distinct authority-bearing route classes with separate witnesses is the narrower candidate. Do not claim invention of partial reuse.

5. **Is scientific-authority separation already standard?**  
   Proposal-vs-acceptance and verifier gates are widespread. Automatically preventing transformation-memory hits from minting theorem/novelty/method-promotion authority inside a pre-candidate math runtime is an integration/governance claim, not invention of epistemic humility.

6. **Does any source contain the entire fail-closed composition under different terminology?**  
   Within the registered search universe: **no**. This is `SYSTEM_COMBINATION_NOT_FOUND_WITHIN_REGISTERED_SEARCH`, not global firstness.

## Honest narrowing

Rejected absolute claims:

- first analogy-based research system
- first transformation memory
- first learn-from-failure system
- first partial-reuse system
- first structural retrieval system
- first invention-last system (without deeper historical search)
- first self-improving research memory

Retained bounded claim:

> fail-closed executable integration/governance contract for content-bound obstruction–transformation routing with authority separation; no single registered source matched the complete contract; strongest-parent composition anticipates much of the conceptual space.

## Residual risk

A deeper historical or industrial-system search could still surface an equivalent unnamed contract. Status remains internal and bounded. Independent external review was unavailable and was not invented.
