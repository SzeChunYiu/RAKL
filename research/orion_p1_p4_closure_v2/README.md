# ORION Papers I–IV strict dependency closure v2

This packet implements the campaign rule that a failed hypothesis is evidence, not active runtime authority.

## Closure invariant

Every `runtime_required=true` node in `DEPENDENCY_GRAPH.json` must terminate in a promotion-grade mechanic. Historical `NEGATIVE`, `CANNOT_CHECK`, `RESOURCE_BLOCKED`, `PROPOSAL_ONLY`, `CANNOT_IDENTIFY`, or `KEEP_PROPOSAL_ONLY` records remain immutable but cannot be the implementation chosen by ORION. A failure therefore triggers one of three moves: diagnose and promote a versioned successor, fall back to a stronger already-promoted parent, or remove the failed hypothesis from the required graph.

## Paper I — Epistemic Mechanics

The approved authority-ledger, epistemic-noninterference, v3 scientific-authority and projection-benchmark subjects are byte-identical between the approved 2026-08-14 freeze and the current-main closure base. Their positive verification therefore transports without evaluator drift. `P1_P2_TRANSPORT_WITNESS.json` contains the exact Git blob identities.

## Paper II — Structural Mechanics

The four-family exact objective verifier and the additive six-family extension are likewise byte-identical to the approved freeze. The approved fail-closed exact-verifier and downstream-routing mechanics remain active. Natural-language extraction or independent-human natural-domain validity are external assurance coordinates, not required authority for the exact-verifier runtime.

## Paper III — Method-Evolution Mechanics

The production SEARCH/JUMP/GLUE/LIFT stack, typed candidate/composition rejection and target-validated consolidation are active on main. The historical model-level experience negative remains in the paper. RSHEA subsequently localized a benchmark construct defect in the old evidence-binding interface and later evidence showed the conceptual model floor was not a valid permanent stopping rule. `P3_STRUCTURED_EXPERIENCE_ACTION_RECEIPT.json` therefore tests the load-bearing dependency directly at the verified structured-state boundary: 1,792 cases, exact action 1.0, unsafe apply 0, CANNOT_CHECK recall 1.0, strongest simple composite ceiling 0.7143, ten of ten hostile mutations caught. The active required experience-to-action node is this promoted structured mechanic; model/natural-language efficacy remains adapter assurance rather than implementation authority.

## Paper IV — Structural Learning Mechanics

The corrected v2 A100 ladder established a 7B state-reachability mechanism signal, but RSHEA development stress found that adaptive-v1 over-concentrates allocation and loses to the static structural parent. That negative is preserved in `P4_ADAPTIVE_DEVELOPMENT_NEGATIVE.json`. `training_policy_authority.py` therefore makes STATIC_STRUCTURAL the active default and permits ADAPTIVE_STRUCTURAL only after an external fresh `ADAPTIVE_RESIDUAL_SUPPORTED` receipt passes strongest-parent residual, hard-harm and full-overhead gates.

The five-arm exact 7B protocol remains useful assurance rather than an active dependency. The prior current-session resource block was localized to execution portability: Phase-1 used an already staged exact 7B revision on LUNARC A100 while the Phase-2 runner requested a Hub cache. `run_phase2_v1_lunarc.sbatch` creates the required offline cache view over that exact staged revision and executes the unchanged frozen Phase-2 module. No outcome is invented in this packet.

## Authority boundary

Promotion here means engineering/mechanic authority inside the explicitly tested scope. It does not mint scientific truth, theorem validity, universal model superiority, independent novelty, or external peer review.
