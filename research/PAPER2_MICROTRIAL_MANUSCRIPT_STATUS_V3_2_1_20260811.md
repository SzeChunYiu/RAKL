# Paper 2 microtrial manuscript status after V3.2 staging

Date: 2026-08-11

The native V3.2 LUNARC stage promoted the frozen CPU environment and emitted a
final-path `STAGING_PASS_ATOMICALLY_PROMOTED` receipt with 38 exact artifacts,
the exact 31-distribution environment, Python 3.11.13 and CPU-only Torch 2.8.0.
The original governed harvest returned `HARVEST_CANNOT_CHECK` because its
freeze parser rejected the exact bundled pip/setuptools direct-reference
representation. Both receipts are retained.

This material result advances asset availability but does not execute the
sealed pendulum microtrial and does not supply a model score, matched-arm
comparison, cost estimate, performance claim or quantitative figure. Counts for
this retry are two staging-only jobs, zero model executions and zero evaluated
result records. Cumulative V3/V3.1/V3.2 staging counts are six jobs, zero model
executions and zero evaluated result records.

The V3.2.1 repair is harvest-only, additive and
`HARVEST_REPAIR_READY_NOT_REHARVESTED`. A chronology-fresh execution packet
remains blocked until the same native scheduler/receipt chain is positively
re-harvested after merge. This status is internal same-context work, not
independent review or peer review.

The cumulative counts and V3.2 staging attestations are machine-bound in
`research/paper2_microtrial_v3/PAPER2_NATIVE_V3_2_SUCCESS_HARVEST_REPAIR_READINESS_RECEIPT_20260811.json`;
no Paper 2 quantitative performance figure is generated from staging evidence.
