# Negative-Result Revival Map

## Paper I — bounded saturation downstream solve-enablement NULL

Historical v1 remains NULL:
35.7% vs 31.3%, p=0.227, n=112.

### Current legal action

Run the already-frozen disjoint/repaired revival exactly as registered.

### If still negative

Do not change alpha.

Root-cause branches:

- insufficient population discordance -> larger fresh population if design is still valid;
- flatness misaligned with downstream decision value -> new VOI-Saturation mechanic;
- retrieval candidates lack useful premises -> Active Fibre Discriminator / representation change;
- Arm-C cost invalid -> repair cost model before any claim.

---

## Paper I — L0/L1/L2/L3 CANNOT_CHECK recall loss

Pattern: safety succeeds but supported incomplete records are refused.

Successor:

`CANNOT_CHECK -> named missing coordinate -> bounded evidence acquisition -> re-evaluate`.

Compare:

- current hard refusal;
- naive fill-in;
- typed active repair.

Safety hard gate must stay at zero unauthorized transition/wrong composition/gluing/upgrade.

---

## Paper II — six-family non-falsifiable PASS

Closed realization. Never rerun.

Absorbed mechanism: falsifiability battery.

---

## Paper II — controlled-witness serialization identity

Closed realization. Never claim extraction.

Absorbed mechanism: text destruction and candidate/public-gold separation.

---

## Paper II — prose template inversion

Root cause: author/template coupling and near-invertible surface.

Successor:
externally authored scientific text + independent representation construction.
No cue-list widening.

---

## Paper II — ARN NEGATIVE__CAPABILITY_ABSENT

Deeper cause hypothesis:

1. current reducer sees surface roles only;
2. far analogies may require schema construction from comparisons;
3. abstraction level is itself uncertain;
4. implicit causality remains hard even for current abstraction pipelines.

Successor after capability authorization:

- one-shot direct model;
- published-style structure-abduction parent;
- strong multi-level abstraction parent;
- RAKL PRA + typed morphism + fail-closed gate.

Outcome must separate:

`extraction`, `abstraction`, `mapping`, `typed licence`, `abstention`.

---

## Paper II — StoryAnalogy sparse-family failures

Retire sparse TF-IDF/ordinal realization.
Do not threshold-shop.

Use as evidence that representation construction is the missing stage.

---

## Framework — Stage-A metric negative

Latest main:
exact GED Spearman = 0.7975 < frozen 0.80.
WL-3 separates 20/20 signature-equal non-isomorphic decoys.

### Revival hypothesis

GED captures graded edit distance but misses some structural equivalence distinctions; WL captures iterative local structure but may lose edit calibration.

Create V2 before any fresh outcome with a complementary vector/lexicographic candidate, e.g.

\[
m(G,H)=
(\mathrm{typed\ constraint\ violations},
\operatorname{GED}(G,H),
\operatorname{WL}_k(G,H),
\mathrm{query\!-\!class\ witness}).
\]

Do not tune a fused scalar after results.

Test the original requirements on fresh Stage-A worlds and later external labels.
