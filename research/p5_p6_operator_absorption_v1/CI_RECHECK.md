# Exact-head CI recheck

Purpose: trigger protected repository and dedicated `failure-analysis-absorption` CI after the previously diagnosed Class-A/integrity fixes.

No algorithm, packet hypothesis, parent, threshold, falsifier, expected terminal, authority field, or evaluated outcome is changed by this marker.

Expected development-only terminal remains `PARENT_SEMANTICS_ABSORBED` if and only if all frozen hard semantics pass.
