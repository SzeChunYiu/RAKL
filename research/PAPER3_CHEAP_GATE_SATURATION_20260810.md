# Paper 3 cheap-gate saturation and residual ledger

Date: 2026-08-10  
Authority: internal same-session research record; not independent review.

## Object, QoI and context

- **Object:** directional validity of a context/QoI-scoped transfer between a source and target structural description.
- **QoI:** incremental held-out discrimination of valid versus invalid transfer beyond semantic, skill and dependency-aware controls.
- **Population:** 44 balanced, constructed Q1--Q4 proposal pairs across 11 proposed mechanism families.
- **Observation boundary:** registered surface terms, skill tags, dependency patterns, QoI, invariant, boundary and directional mapping fields.
- **Authority boundary:** all labels are same-session AI proposals; zero items are independently annotated or adjudicated.

## Atomic chain and active fibers

1. Preserve benchmark/evaluator/result chronology.
2. Test annotation authority before confirmatory promotion.
3. Map surface, skill and dependency representations before competition.
4. Fit family-held-out parent controls.
5. Add witnessed-structure fields and read the incremental residual.
6. Apply Q2/Q3 safety and calibration gates.
7. Let the independent-annotation gate govern scale authorization.

The annotation-authority fiber remains open. The locally fitted predictor fiber produced a diagnostic observation, but cannot close the authority fiber.

## Parent assimilation and representation map

The evaluated non-witness sequence was semantic overlap -> skill-tag overlap -> dependency-pattern overlap. The dependency-aware predictor was the strongest parent control on the constructed proposal set. The RAKL challenger added preserved-invariant, target-boundary, QoI and directional-completeness coordinates. This is a feature-representation comparison, not mechanism identification and not a foundation-model transfer experiment.

## Exact observation

The frozen internal diagnostic passed its numeric signal thresholds versus the dependency-aware control: ROC-AUC gain 0.0857, average-precision gain 0.0972 and Brier reduction 0.1270; witnessed Q2 true accept was 1.00 and Q3 false accept was 0.00. These values describe internally constructed proposal labels only.

The higher-authority annotation gate failed: 0/44 cases are confirmatory-eligible. Verdict: `FAIL_CLOSED_MISSING_INDEPENDENT_ANNOTATION`. No expensive training or inference was authorized or launched.

## Candidate frontier and claim narrowing

Retained frontier:

1. deterministic three-family conformance implementation;
2. broader eleven-family internally constructed diagnostic;
3. preregistered confirmatory and efficiency programme.

Promoted claim: the implementation and frozen diagnostic can express and test the intended boundary-aware distinction on constructed proposal worlds.

Not promoted: independent label validity, natural cross-domain generalization, foundation-model transfer, training efficiency, inference efficiency, shared-substrate identity or economic break-even.

## Residuals and next recursion

- `P3-EMP-01`: obtain >=2 genuinely independent human/expert annotations plus adjudication for confirmatory items.
- `P3-EMP-02`: rerun strong semantic/LLM/skill/dependency-aware controls on that frozen benchmark.
- `P3-EMP-03/04`, `P3-ECON-01`: remain dormant until the overall cheap gate passes.

Knowledge saturation is not positive-goal closure. The cheapest discriminating next observation is independent annotation, not GPU training. The gate must be rerun without changing the frozen thresholds; a failure narrows the paper further rather than rescuing the claim.
